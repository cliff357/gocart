rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============================================
    // Helper Functions
    // ============================================
    
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isAdmin() {
      return isAuthenticated() && 
             exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }
    
    // 檢查是否有待處理的邀請
    function hasPendingInvite() {
      return isAuthenticated() && 
             exists(/databases/$(database)/documents/adminInvites/$(request.auth.token.email));
    }
    
    // ============================================
    // Users Collection - 只有受邀請用戶可創建
    // ============================================
    match /users/{userId} {
      allow read: if true;
      
      // 只有受邀請的用戶可以創建自己的文檔
      allow create: if isAuthenticated() && 
                       request.auth.uid == userId &&
                       hasPendingInvite();
      
      // Admin 可以更新任何用戶，用戶可以更新自己
      allow update: if isAdmin() || 
                       (isAuthenticated() && request.auth.uid == userId);
      
      allow delete: if isAdmin();
    }
    
    // ============================================
    // Admin Invites Collection - 特殊規則
    // ============================================
    match /adminInvites/{inviteId} {
      allow read: if true;
      
      // Admin 可以創建邀請
      allow create: if isAdmin();
      
      // 被邀請的用戶可以刪除（接受邀請時）
      allow delete: if isAdmin() || 
                       (isAuthenticated() && 
                        resource.data.email == request.auth.token.email);
    }

    // ============================================
    // Reservations Collection - 訪客可創建預訂
    // ============================================
    match /reservations/{reservationId} {
      // 只有 Admin 可以讀取預訂列表
      allow read: if isAdmin();
      
      // 任何人都可以創建預訂（訪客預訂功能）
      allow create: if true;
      
      // 只有 Admin 可以更新和刪除預訂
      allow update, delete: if isAdmin();
    }

    // ============================================
    // Settings Collection - 公開讀取
    // ============================================
    match /settings/{settingId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ============================================
    // Products Collection - 公開讀取
    // ============================================
    match /products/{productId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ============================================
    // Categories Collection - 公開讀取
    // ============================================
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ============================================
    // Ratings Collection - 公開讀取
    // ============================================
    match /ratings/{ratingId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ============================================
    // Orders Collection - 只有 Admin 可讀寫
    // ============================================
    match /orders/{orderId} {
      allow read, write: if isAdmin();
    }

    // ============================================
    // Addresses Collection - 需要登入才可讀
    // ============================================
    match /addresses/{addressId} {
      allow read: if isAuthenticated();
      allow write: if isAdmin();
    }

    // ============================================
    // Coupons Collection - 只有 Admin 可讀寫
    // ============================================
    match /coupons/{couponId} {
      allow read, write: if isAdmin();
    }

    // ============================================
    // Stores Collection - 只有 Admin 可讀寫
    // ============================================
    match /stores/{storeId} {
      allow read, write: if isAdmin();
    }
    
    // ============================================
    // 默認規則：拒絕所有未明確定義的集合
    // ============================================
    match /{document=**} {
      allow read, write: if false;
    }
  }
}